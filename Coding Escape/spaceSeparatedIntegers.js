// Read the number of integers 
const N = parseInt(readline());

// Read the space-separated integers 
var inputs = readline().split(' ').mapNumber

// Sort the array in ascending order 
inputs.sort((a,b)=>  a-b);

// print the sorted array 
console.log(inputs.join(' '));